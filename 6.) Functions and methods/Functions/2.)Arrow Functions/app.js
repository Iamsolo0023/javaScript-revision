// Arrow function is a compact way of writting a function.
const addFunction= (a,b,c) =>{
    return a + b + c;
}
let addResult = addFunction(1,2,3);
console.log(`The sum of 1,2,3 is ${addResult}`);